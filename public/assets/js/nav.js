


/*
const navLinks = document.querySelectorAll('nav-link');
// Agregar evento de clic a cada enlace
navLinks.forEach(link => {
  link.addEventListener('click', function(event) {
    event.preventDefault(); // Evitar que se realice la acción predeterminada del enlace
    navLinks.forEach(link => link.classList.remove('clicked')); // Remover la clase 'clicked' de todos los enlaces
    this.classList.add('clicked'); // Agregar la clase 'clicked' al enlace que se ha hecho clic
  });
});
*/
